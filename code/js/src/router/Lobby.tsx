import * as React from "react"
import { GameType } from "../domain/GameTypes"
import { askService } from "../service/askService"
import { service } from "./App"

export function EnteredGame() {
    //const gameId = askService(service, service.enteredGame)

    if (true) {
        return (
            <div>
                Waiting for oponent...
            </div>
        )
    }
    
    return (
        <div>Navigate to game layout def</div>
    )
}