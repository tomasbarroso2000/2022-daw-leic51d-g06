export class Square {
    row: string
    column: number
    firstRow: string
    firstColumn: number
    constructor(row: string, column: number) {
        this.row = row
        this.column = column
        this.firstRow = "a"
        this.firstColumn = 1
    }
}